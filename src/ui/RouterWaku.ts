/**
 * @module ui/RouterWaku
 *
 * Re-export of `last-ts/Waku` — Waku location Layer + hooks.
 * Soft-nav: {@link Router.link}`(catalog)` from `last-ts/Router`.
 * Prefer `Last.provider(Waku.fromApi|layer)` over a nested Provider.
 */
export * from "last-ts/Waku";
