/**
 * @module ui/runtime
 *
 * Re-export of `last-ts/AtomReact` runtime context. Prefer
 * `import * as AtomReact from "last-ts/AtomReact"`.
 */
export {
  type AnyRuntime,
  RuntimeProvider,
  useRuntime,
} from "last-ts/AtomReact";
