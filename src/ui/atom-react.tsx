/**
 * @module ui/atom-react
 *
 * Re-export of `last-ts/AtomReact` registry hooks. Prefer
 * `import * as AtomReact from "last-ts/AtomReact"`.
 */
export {
  RegistryProvider,
  useAtomValue,
  useAtomSet,
  useAtomMount,
  useAtomRefresh,
} from "last-ts/AtomReact";
