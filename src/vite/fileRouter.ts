/**
 * @module vite/fileRouter
 *
 * Re-export of `last-ts/vite`. Prefer `import { fileRouter } from "last-ts/vite"`.
 */
export {
  fileRouter,
  checkPaths,
  discover,
  emitPaths,
  nodeLayer,
  runSync,
  type FileRouterPluginOptions,
} from "last-ts/vite";
