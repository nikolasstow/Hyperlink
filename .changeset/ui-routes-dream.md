---
"hyperlink-ts": minor
---

**UI Route (HttpApi-shaped):** `Route.make` / `group` / `get` / `urlBuilder` / `match` / `reflect`. Root endpoints on the api; optional `topLevel` on groups. `Route.addHttpApi` mixes Effect `HttpApi` path trees into the router (URL surface only). No Group.Service route helpers.
