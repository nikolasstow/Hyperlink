---
"last-ts": minor
---

Router-scoped context (track 2): catalog/group `.context(Ctx)`, `Last.provideContext` to discharge builder debt (Layout dual), active-path bag mounts in `Router.Outlet`, and `Last.use(App)` / `Last.use(App, "docs")` / selector overloads.
