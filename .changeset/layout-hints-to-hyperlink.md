---
"last-ts": minor
"hyperlink-ts": minor
---

Pane metadata stays on Hyperlink as `Chrome` / `ChromeProvider` / `useChrome` (`hyperlink-ts/ui/View`). Removed from last-ts (codesplit). Hyperlink keeps `Views.SizeChrome` and `tui/chrome` — chrome is fine on Hyperlink; banned only on last-ts layout/context surfaces.
