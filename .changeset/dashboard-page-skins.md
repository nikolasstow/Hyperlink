---
"hyperlink-ts": minor
---

Add `WorkPoolView.PoolPage` and `DaemonView.DaemonPage` View handles; web/TUI Page skins for `/…/logs` and `/…/schedule`. Batteries shell routes those paths through `Match.Page` (`web/resourcePages` hosts LogBox / LogsPage / SchedulePage).
