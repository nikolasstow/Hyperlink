---
"hyperlink-ts": minor
---

**UI Router refine:** `go`/`to` accept `{ replace }`; Group `up`/`toRoot` replace (history stays coherent with `back`). Group catalogs stamp `Route.Target` for match-driven `selected`/`view`. Group helpers fail loud on bare `Route.Api` layers. Impl split to `internal/uiRouter`.
