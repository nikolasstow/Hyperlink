---
"hyperlink-ts": minor
---

Navigator root shell pages for node status: `openHealth` / `openNode` → `/health` and `/health/<nodeId>`. Batteries web `DashboardShell` routes HealthBoard / NodeDetail through those paths (`NodeStatusHost` remains for overlay embeds).
