---
"hyperlink-ts": minor
---

ViewKind is now `Data.TaggedEnum` (`ViewKind.Card()` / `.Detail()` / `.Page()` with PascalCase `_tag`); match with `Match.tag`. String sizes `"card" | "detail" | "page"` removed.
