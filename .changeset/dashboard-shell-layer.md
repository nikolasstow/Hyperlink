---
"hyperlink-ts": minor
---

**Dashboard stack:** shared `DashboardLayer.forCompose` (contributions + skins + `View.base`) and platform `DashboardShell` (web/TUI). `<Dashboard />` is thin wiring over compose + shell — kit batteries API remains HOLD. Examples keep the one-liner.
