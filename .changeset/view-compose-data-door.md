---
"hyperlink-ts": minor
---

`View.compose` exposes `ui.data` (shared `RuntimeProvider` in `ui/runtime`) — same `*Bundle` builders as Dashboard (`queue` / `daemon` / `api` / …). Web + TUI re-export the shared runtime context.
