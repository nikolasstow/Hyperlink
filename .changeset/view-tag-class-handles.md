---
"hyperlink-ts": minor
---

**View DI:** `View.Service` / **`View.Prototype`** — component Context.Service handles (props from Prototype; Self = DI identity; `View.Type<typeof Tag>`). Svc type is **`View.View<Props>`** (defaults to `ViewProps`; was `ViewFn` / `ViewComponent`). Size chrome add-on: **`View.Card` / `Detail` / `Page` prototypes** stamp `size`. Matchers on **`View.react` / `compose` kits** (`ui.Card`) and **`View.useMatch()`**. Chrome registration: `View.bind` + `View.only`.

**Dashboard:** optional `views` Layer (`R = View.Registry`) merges under shipped skins — app Prototypes via `View.only` / `Layer.provide(Layer.succeed(…))` (see hyperlink-web WorkerPool).
