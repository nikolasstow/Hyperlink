---
"hyperlink-ts": patch
---

Add runnable Lookup same-address A→B ownership handoff recipe (`examples/node/lookup-follow-handoff.ts`) — orchestrator forks B bind-retry, releases A, `Lookup.follow` dialers land on B (cold registry).
