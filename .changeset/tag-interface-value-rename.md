---
"hyperlink-ts": minor
---

Fold interface-driven tags into `Hyperlink.Service<Self, I>()` (remove `fromService`). Rename materialize `constant(Schema)` → `value(Schema)` with optional acquire error (fails layer/client build).
