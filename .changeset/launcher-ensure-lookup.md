---
"hyperlink-ts": minor
---

Add `Launcher.ensureLookup` / `UpOptions.lookup` — ensure-Lookup-first (adopt live Lookup or spawn Lookup-only child; fail closed with `LookupNotRunning` / `LookupAddressRequired`). `Lookup.layerNode` accepts `assumeToken` for custody. Soft-bake stays independent-launch only.
