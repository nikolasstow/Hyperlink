---
"hyperlink-ts": minor
---

**UI Router:** `Route.handle` attaches a render function to a destination; `Router.Outlet` renders the matched handle (params + pathname). Real match→render path for any app.
