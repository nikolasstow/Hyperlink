---
"hyperlink-ts": patch
---

Launcher Effect-fullest pass: `Encoding.encodeHex` token mint, `Semaphore` single-flight on Handle phases, `Schedule`/`Duration` Ready poll, `Option`/`Predicate.isTagged` error narrowing, OTEL `withSpan` alongside log spans, `Effect.forEach` for `up`, and `Launcher.readyTimeoutConfig` (`HYPERLINK_LAUNCHER_READY_TIMEOUT`).
