---
"hyperlink-ts": minor
---

Track A launcher: `hyperlink-ts/Launcher` (`spawn` / `awaitReady` / `handoff` / `up`) and `Node.assume({ token })` ownership ack with Ready-gated handshake, tagged assume errors, and optional `ListenOptions.assumeToken` / `Node.assumeTokenConfig`.
