---
"hyperlink-ts": minor
---

Add `Lookup.planUpdate` — dry-run update impact (Directory co-update, Advice-prefer clientsAtRisk proxy, Versioned migration gaps, Spec wireRemovals, status contract drifts). Fail-closed `UpdateBlocked` unless `{ force: true }`. Launcher stays spawn-only; `restartSuccessor` still deferred.
