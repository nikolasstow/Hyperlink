---
"hyperlink-ts": minor
---

**UI Route dream machine:** typed `Route.urlBuilder` / `Router.make` (HttpApiClient-shaped generics through `.add`). `Router.Link`, `Router.ForApi`, Target-as-SSOT resolve. Group `up`/`toRoot` replace; helpers fail loud on bare catalogs.
