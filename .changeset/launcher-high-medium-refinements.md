---
"hyperlink-ts": patch
---

Launcher high/medium refinements: fail-closed kill on `ReadyTimedOut` + `Handle.kill`; Tag-typed `ready.services`; Config resolved at `spawn` (no `ConfigError` on Handle); branded `Token`; `Launcher.layer`; `command` `tokenArgvAt`; `Launcher.entry`; `up` concurrency option.
