---
"hyperlink-ts": minor
---

**UI Router:** `hyperlink-ts/ui/Router` — `Context.Service` over a `Route.Api` with swappable `memory` / `history` layers (`go` / `to` / `back` / `match` / `subscribe`). Catalog stays data (`Route.make…`); transport is the layer.
