---
"hyperlink-ts": minor
---

Add `Hyperlink.promise(handle)` — Promise/async adapter over a service handle (`Effect`→`Promise`, `Stream`→`AsyncIterable`, `ref`→`PromiseSubscribable`). OS edge for non-Effect consumers; not a Spec leaf.
