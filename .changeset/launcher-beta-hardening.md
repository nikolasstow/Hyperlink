---
"hyperlink-ts": patch
---

Launcher beta hardening: CSPRNG assume tokens, `HandleNotReady` before handoff, richer tagged-error messages, Effect log spans/annotations on spawn / Ready / handoff / `Node.assume` (tokens never logged), and listen binds stamp the real node key onto assume errors/logs.
