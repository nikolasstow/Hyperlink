---
"hyperlink-ts": minor
---

Add `Hyperlink.atom` / `.query` / `.fn` (Effect-reactive adapters over Tag or live source) and `Bundle.observe` (`hyperlink-ts/ui/Bundle`) as the thin-handle UI observe door over existing `*Bundle` builders.
