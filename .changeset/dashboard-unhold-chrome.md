---
"hyperlink-ts": minor
---

Unhold kit `<Dashboard />` as the supported one-liner over `DashboardLayer.forCompose` → `View.compose` → `DashboardShell`. Export public chrome: web/TUI `DashboardTopBar` and web `DashboardDetailChrome` (`NodeBar` / `HealthBoard` / `NodeDetail` remain exported shell overlays for v1).
