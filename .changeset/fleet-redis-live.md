---
"hyperlink-ts": patch
---

Optional peer `ioredis` for Effect `NodeRedis` fleet rate limiting. Live Redis proof (Gate/WorkPool shared budget + child-process peer; Effect `Persistence.layerRedis` / `PersistedQueue.layerStoreRedis`), `docker-compose.redis.yml`, and fleet demo Redis auto-detect.
