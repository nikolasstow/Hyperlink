---
"hyperlink-ts": minor
---

Gate `metrics` nest: live `remaining` / `resetAfter` / `exceeded` on Tag/Service handles (updates when `rateLimit` is set). Stable Tag metadata helpers `Gate.rateLimitKeyOf` / `Gate.metricsKeyOf` for widget discovery outside the nest path.
