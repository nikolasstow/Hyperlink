---
"hyperlink-ts": minor
---

Lookup Directory membership push: `Directory.changes` stream (`DirectoryUpserted` with `dialChanged` on A→B dial swap, `DirectoryRemoved` on unregister) plus `Lookup.changes` / `Lookup.directoryTable` sugar for peer rebind hooks.
