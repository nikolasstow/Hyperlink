---
"hyperlink-ts": patch
---

Launcher cleanup + docs + thorough tests: Token brand lives in mint path; Handle.kill / ReadyTimedOut spend; Tag/wire `ready.services`; Config-at-spawn; `layer` / `entry` / `tokenArgvAt` / `up` concurrency covered in suites and guide.
