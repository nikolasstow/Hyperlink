---
"hyperlink-ts": minor
---

Nameless listens accept Tag/`protocolHttp` address shorthand: `Node.http(serves, 3000 | ":3000" | url)`, same for `ws`, and path strings for `unix` / `nPipe`. Object `{ port, url, … }` remains.
