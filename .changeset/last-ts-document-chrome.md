---
"last-ts": minor
---

Add `last-ts/Document` (`make`, `provide`, `transform` sugars, `Head`) and `Page.document` for typed head chrome; `Layout.Outlet` / `Layout.Root` for root html. Never yield inside `()`.
