---
"last-ts": minor
"hyperlink-ts": minor
---

Complete last-ts codesplit P2–P3: move Route/Router (+ waku), View DI kernel, and docgen into `last-ts`. Hyperlink keeps Route Target helpers, kind-aware View `base`, and `Ui` (compose / GroupDash). `@nikscripts/docgen` becomes a thin re-export of `last-ts/docgen`.
