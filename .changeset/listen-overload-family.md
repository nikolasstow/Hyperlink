---
"hyperlink-ts": minor
---

Align `Node.http` / `ws` / `unix` / `nPipe` overload family: unbound Tag+impl (nameless), Tag+impl+Node, single serve without brackets, and node+single serve — same shapes across siblings.
