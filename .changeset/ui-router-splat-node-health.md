---
"hyperlink-ts": minor
---

**UI Router:** named splat params (`/health/*nodeId`) keep `/` in values (`urls.nodeHealth("app/NodeA")` → `/health/app/NodeA`). Group `asRoutes` renames `healthNode` → `nodeHealth`.
