---
"hyperlink-ts": patch
---

Nameless `Node.http` / `ws` / `nPipe` Soft-bake `Lookup.layer` when Identity is absent (same as `unix`) so no node / no address / no Lookup pipe still claims and advertises.
