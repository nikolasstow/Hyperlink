---
"hyperlink-ts": minor
---

Track B membership: `ListenOptions.onYield` configures node-status `yield` for Lookup `askIncumbent` (directory-row replace only). Custody stays on `Launcher`; Lookup stays Identity/Directory/Advice after assume — no blank-worker assign protocol.
