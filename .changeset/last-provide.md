---
"last-ts": minor
---

`Last.provide(effect)` / `Last.provide(effect, requirements)` — entry-point `Effect.provide` + run. Services are Effects: `Last.provide(Hello, Hello.layer)`. Bag `yield* Last.provide` / `Last.toLayer` removed (Document owns title).
