---
"hyperlink-ts": minor
---

`hyperlink-ts/ui/Router/waku` is the Waku **layer** only (`waku` / `layer.waku` + `Provider` / `Link` / `useRouter` / `setDefault`) — not a mirror of the lite Router namespace. Memory / History / `Outlet` / `make` / the `Router` Context tag stay on `hyperlink-ts/ui/Router`.
