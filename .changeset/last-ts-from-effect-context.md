---
"last-ts": minor
---

`group.effect` / `groupsEffect` always defer; item types stay on the catalog for UrlBuilder (including path-arg arity); requirements `R` unions onto `Api` and `RouterBuilder.layer`. Materialize with `resolveApi` / layer (sync helpers only when `R = never`).
