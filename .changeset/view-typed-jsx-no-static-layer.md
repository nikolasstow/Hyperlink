---
"last-ts": patch
---

**Views teaching:** defaults via `View.make(key, default)` + const `Layer.effect` /
`Last.provide(Tag, layer)` — do not teach `static layer` / `Tag.layer`. Demo SSOT is
`examples/ui/view-typed-jsx/` (`ui/` leaves, `lib/` composition); docs Twoslash +
island import the same files.
