---
"hyperlink-ts": patch
---

Add live `examples/launcher/restart-successor` — OS A→B via
`Launcher.restartSuccessor` (same-`nodeKey` dial-replace). Blocked-plan demo
stays at `examples/launcher/plan-update`.
