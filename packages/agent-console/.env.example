# Default is "/opencode", proxied to the local opencode server by vite.config.ts —
# leave unset unless you're pointing at an opencode server that ISN'T the one this
# dev server proxies to (e.g. a different host/port).
# VITE_OPENCODE_BASE_URL=http://127.0.0.1:4096
