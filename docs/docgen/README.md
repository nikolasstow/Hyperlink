# @nikscripts/docgen (shim)

Implementation moved to **`last-ts/docgen`**. This package re-exports for existing
`@nikscripts/docgen/*` imports (docs site). Prefer:

```ts
import * as Extractor from "last-ts/docgen/Extractor"
```
